# HAPI FHIR US Core Server - Environment variables
# Copy to .env and adjust as needed

# PostgreSQL password
POSTGRES_PASSWORD=admin

# PostgreSQL host port (default: 5433 to avoid conflict with host's 5432)
POSTGRES_PORT=5433

# FHIR server port (default: 8023)
FHIR_PORT=8023

# Terminology config (default: application.yaml = multi-terminology, tx + VSAC)
# SPRING_CONFIG_ADDITIONAL_LOCATION=file:///config/application.yaml   # default
# SPRING_CONFIG_ADDITIONAL_LOCATION=file:///config/application_default.yaml   # single remote tx only
# SPRING_CONFIG_ADDITIONAL_LOCATION=file:///config/application-local-terminology.yaml   # local only (run load-terminology.sh first)

# MCP (Model Context Protocol) server - for AI/LLM integration (VS Code, Claude, etc.)
# Default port: 8000. Set empty to disable or override.
MCP_PORT=8000

# VSAC terminology (multi-terminology config + proxy)
# Get a free UMLS API key: https://uts.nlm.nih.gov/uts/profile
# VSAC_UMLS_API_KEY=your-umls-api-key
# VSAC_TERMINOLOGY_URL=http://vsac-proxy:8081/
# To run the proxy standalone: ./run-vsac-proxy.sh (reads VSAC_UMLS_API_KEY from .env)
